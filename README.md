# Vanilla (HTML/JS) MFE Template

Minimal vanilla single-spa app using webpack (SystemJS output).

Placeholders:
- playground-vanilla
- org
- 9008

Steps:
1) Copy this folder into `apps/<new-vanilla>`
2) Replace placeholders in all files
3) Add import map + layout entries in root-config
